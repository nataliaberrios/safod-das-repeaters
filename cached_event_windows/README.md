# Cached event windows

Small, provenance-tracked waveform subsets used by the advisor notebook live
here.  They are generated by v2 scripts from read-only HDF5 or NCEDC inputs.
Never place raw archives here.  Each NPZ must include UTC origin/window, sample
rate, channel/locus indices, source files or request URL, processing parameters,
and the pilot configuration hash.

