# Cached public catalogs

Large public catalog files are downloaded here only for local rebuilds and are
ignored by Git. Small exact-ID crosswalks and checksum-bearing provenance files
are written to `outputs/external_validation/`.
